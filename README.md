# TO RUN IN WEBSITE : use your local host and in the terminal type - { npm start }
# to run in APP :
   # steps too be followed :-
   1. download EXpo - go app on your phone 
   2. open terminal in laptop and type command is : { node server.js } and { npx expo start }
   3. scan the qr -code or set locallhost for it
